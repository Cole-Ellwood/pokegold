# Current Project Roadmap

## Boss AI Cognition Mode

For Boss AI work, the roadmap should not domesticate the thinking. Record the
wildest fair ideas, the scary boss-feel questions, and the next deep branch, but
keep implementation rows honest about proof, memory, and no-cheat boundaries.

This is the broad work board for future Codex/helper sessions. Use it after
`docs/codex_context.md` and `docs/project_map.md` when deciding what the current
project is and what still needs attention.

Update rule: every session that touches a listed workstream must update its row
when work is completed, started, blocked, deliberately left untouched, or when a
useful idea appears. Keep ideas in `Ideas / next moves`; do not invent new status
values for speculation.

Status legend:

- `UNTOUCHED`: intentionally not started yet.
- `IN_PROGRESS`: started, but not fully verified or finished.
- `STUCK`: blocked by missing input, missing tooling, unclear source truth, or a
  required manual/runtime step.
- `COMPLETE`: finished and verified enough for the evidence listed.

Source-truth rule: this roadmap is an index of current work, not an authority
over source. If it conflicts with source files, linker outputs, or generated
audits, trust the source truth and update this file.

Felt-promise rule: every workstream must name the experience it protects. Beauty
is first evidence that the underlying order is right, then a force that makes
future helpers trust, linger, and protect the work. If a change is technically
correct but makes the hack feel cheap, arbitrary, noisy, dead, or less like fair
hard Pokemon Gold, treat that as a real defect and record it here.

First-Playthrough Promise: roadmap work protects the feeling of being small in
Johto again. The hack should restore uncertainty for a veteran player: bosses
can beat you without cheating, forgotten Pokemon can surprise you, and QoL
respects the journey instead of dissolving it.

Urgency order is top-to-bottom: `DO NOW`, `NEXT`, `SOON`, `WAIT`,
`REFERENCE`. Urgency is separate from status. A complete row can still be
important, but it belongs lower once it is no longer the next decision.

| ID | Urgency | Workstream | Status | Last update | Protected promise | Evidence | Stuck on | Ideas / next moves |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MEGAURGENT-001 | `DO NOW` | First real boss-fight proof capsule | `COMPLETE` | 2026-04-26 | The hack should not call itself fair-hard from static audits alone; at least one current trace ROM boss fight needs live decision proof. | Morty now has current live move-decision proof. `audit/boss_ai_trace/live_capture_manifest.json` pins trace ROM hash `639680604270248FEC0FF9DDD92205C75FC2B01858FD1A365FBF51623DC66C29` and trace symbol hash `73A70E9C9ADB65840DBD05C9B9049BFF288AF0D2C1EFA22DE375CA773CCE777B`, keeps Morty's `preflight.expect = morty` guard, and records `.local/tmp/morty_issue_cycle8/chosen_frame_3086.state`. `audit/boss_ai_trace/morty_live.txt` has `chosen_id=95`, `top_moves=HYPNOSIS:1,CURSE:20,NIGHT_SHADE:20`, `plan_id=2`, and matching trace hashes. `python tools\audit\check_boss_ai_live_capture_ledger.py` passes with Morty marked `FINISHED`. | None for the first proof capsule. Other boss captures remain separate BOSSAI validation work. | Keep the bad frame-161 and delta-263 states out of manifest proof. Use the Morty proof as the first live capsule, then continue Jasmine/Clair/Koga/Lance/shared switch-loop captures without treating this one proof as whole-game validation. |
| VERIFY-001 | `DO NOW` | Verify, build, document, checkpoint workflow | `IN_PROGRESS` | 2026-04-26 | Stopping points feel trustworthy: the next helper can tell what was proven, what merely compiled, and what still needs play. | 2026-04-26 stopping check passed `python tools\audit\check_docs_navigation.py`, `python tools\audit\check_release_smoke.py`, `python tools\audit\check_ai_tiers.py`, `python tools\audit\check_boss_items_present.py`, `python tools\audit\check_boss_moves_complete.py`, `python tools\audit\check_boss_ai_no_cheat.py`, `python tools\audit\check_boss_ai_gating.py`, `python tools\audit\check_boss_ai_trace_invariants.py`, `python tools\audit\check_boss_ai_memory_budget.py`, `python tools\audit\check_boss_ai_live_capture_ledger.py`, `python tools\audit\check_battle_math_safety.py`, trace helper symbol/watch smokes, `python tools\trace\boss_ai_trace_batch.py` dry-run, `git diff --check`, and WSL `make ... pokegold.gbc pokesilver.gbc` reporting both targets up to date. Follow-up cursor fixes rebuilt normal Gold/Silver plus `pokegold_trace.gbc`, then `python tools\trace\boss_ai_trace_batch.py --execute --only morty` wrote current Morty proof. | No checkpoint/stage step was requested; dirty source ownership is understood but not committed. Additional non-Morty captures are still missing, so broader gameplay proof is incomplete. | Before checkpointing, decide whether the Boss AI cursor fixes plus Morty proof/docs should be one commit or split into source/audit/docs commits. |
| NAV-001 | `REFERENCE` | Agent navigation beauty pass | `COMPLETE` | 2026-04-26 | Future sessions find the correct work surface in constant time, then spend their attention on the actual problem instead of rediscovering the repo. | Added `docs/agent_navigation/` with one-screen start card, navigation health check with smoke routes, doc-role ownership map, task routing, source/generated/output ownership policy, verification matrix, artifact catalog, custom terms glossary, entrypoint links, and subsystem micro-indexes for Boss AI trace, trainer/boss rosters, Pokemon balance, QoL/map scripts, and checkpoint/handoff flow. Follow-up simplification folded the fresh-session dry run into `docs/agent_navigation/navigation_health_check.md`, merged path classification into `docs/agent_navigation/source_output_ownership.md`, removed the duplicated subsystem README, and trimmed repeated micro-index verification command blocks. `tools/audit/check_docs_navigation.py` now auto-discovers `docs/agent_navigation/**/*.md` and validates navigation path references, Python command paths, planned missing live-capture artifacts, and core contract snippets. | None for the core navigation layer. This was docs/tooling-only and did not reorganize source files, generated outputs, or ROM behavior. | Keep as reference. Reopen only if a future prompt shape cannot route through `docs/agent_navigation/start_card.md`, `docs/agent_navigation/task_router.md`, or a subsystem micro-index before broad source search. |
| POLISH-001 | `REFERENCE` | 10/10 repo polish and workspace hygiene | `COMPLETE` | 2026-04-26 | The repo feels physically calm without lying about ROM-hack reality: raw folder browsing should explain itself, not hide source truth or break old build assumptions. | 2026-04-26 polish pass followed `outbox/repo_10_10_polish_prompt.md`: required docs were read, tracked source was clean, `git clean -ndX` found 4247 ignored candidates, `git check-ignore -v` confirmed representative output ownership, and `rg` showed root `.gbc`/`.map`/`.sym` assumptions in docs/tools/scripts/Makefile. Added `tools/audit/check_workspace_hygiene.py`, clearer `.gitignore` ownership comments, and navigation/docs pointers so ignored graphics/RGBDS/build/scratch noise is classified before cleanup. Follow-up organization moved root QoL research to `docs/qol_research_report.md`, deleted a stale ignored sprint12 workspace patch after inspection, and documented `.local/` / `dist/` ownership without touching outputs. | None for docs/tooling hygiene. Physical clutter remains intentionally because root linker outputs, trace/debug outputs, `.local/` probes, tracked release artifacts, and old-school generated graphics are explainable and no output relocation was approved. | Keep as reference. Reopen only with a concrete cleanup/output-relocation plan naming exact files, rebuild commands, dev-index/release-smoke impact, restore path, and user approval before destructive commands. |
| VISION-001 | `REFERENCE` | First-playthrough promise wording | `COMPLETE` | 2026-04-26 | Future sessions remember the deep goal: not generic hard mode, but Pokemon Gold made unknown and scary again for a veteran player. | Core wording added to `docs/README.md`, `docs/codex_context.md`, `docs/project_map.md`, Boss AI, balance, QoL, review, authoring, and agent-navigation docs. | None for docs-only intent propagation. | Keep as reference. Reopen if a future doc or implementation plan frames the hack mainly as difficulty, competitive optimization, or modernization instead of restored uncertainty. |
| BOSSAI-001 | `NEXT` | Boss AI design-promise validation | `IN_PROGRESS` | 2026-04-26 | Bosses feel like strong legal opponents: pressure without clairvoyance, and surprises that make sense afterward. | `docs/boss_ai_spec.md`, `docs/boss_ai_bug_testing_plan.md`, `engine/battle/ai/boss.asm`, trace/audit tools, and 2026-04-26 automated passes for no-cheat, gating, trace invariants, memory budget, boss items, boss moves, and AI tiers. Morty now has current live proof in `audit/boss_ai_trace/morty_live.txt`: `chosen_id=95`, `top_moves=HYPNOSIS:1,CURSE:20,NIGHT_SHADE:20`, `plan_id=2`, and matching current trace hashes. Decision breadth is documented in `docs/boss_ai_spec.md`: late bosses should notice up to about 8-9 immediate actions but deeply project only 4 candidate plays through 3 player reply buckets. Haki is now documented as a future once-per-battle pure-cheat exception: unfair hidden/current-turn reading is allowed only while spending Haki, and must be traced/quarantined. | Jasmine, Clair, Koga, Champion Lance, Red if added, and shared switch-loop live captures still require current trace-ROM save-states or manual debugger positions. Haki has a design contract but no source implementation. | Use `outbox/boss_ai_new_thinker_prompt.md` for the next deep-thinking session. Automated audits plus Morty support the fairness promise, but one boss is not whole-game proof. Capture real boss fights or debugger traces for Jasmine, Clair, Koga, Lance, Red, and shared switch-loop cases. If Haki is implemented later, start with a narrow `BossAI_SwitchOrTryItem` branch unless the move-choice override path is explicitly designed and traced. |
| DIFFICULTY-001 | `NEXT` | Cheap-difficulty audit | `UNTOUCHED` | 2026-04-26 | Losses feel deserved and instructive, not like the designer punished the player for trusting the game. | Project intent in `docs/codex_context.md`; review route in `docs/codex_review_playbook.md`. | Not started. | Look for difficulty that feels cheap: hidden-information reads, forced grinding pressure, unfair item/team spikes, or deterministic boss scripts. Start after at least one live boss proof, because the first proof may reveal whether the cheapness risk is real or only theoretical. |
| QOL-001 | `SOON` | QoL weirdness and friction pass | `IN_PROGRESS` | 2026-04-26 | Friction reduction feels like respect for the player's time and hands without erasing preparation or Gen 2 texture. | `docs/qol_handoff.md` tracks implemented QoL and remaining candidates; `docs/qol_research_report.md` holds the longer research rationale for keeping decision costs while killing clerical costs. | Repel renewal and Pokemon Center trimming still need careful edge-case testing before implementation. | Custom item text and Day-Care signage can happen soon because they clarify existing systems. Repel renewal and Pokemon Center trimming can wait until boss proof/checkpoint work is not competing for attention. |
| BOSSAI-002 | `WAIT` | Gym scout dossier system | `UNTOUCHED` | 2026-04-26 | Gym leaders feel prepared because the world watched the player, not because the ROM cheated. | Design idea from 2026-04-26 thinking pass: every gym trainer can report only visible player battle facts to that gym's leader, and the player should be able to inspect the resulting dossier before the boss. | Not started; needs a tiny Morty-only prototype before committing to all gyms. | Wait until existing Boss AI has live proof. Then prototype in Ecruteak only: record coarse public facts from gym-trainer battles, show the report through gym-guide or leader-adjacent text before Morty, and seed Morty's boss AI with only those public facts. Expand only if it feels fair, tense, and useful for hidden weak-Pokemon utility. |
| HANDOFF-001 | `WAIT` | Final durable handoff artifact | `UNTOUCHED` | 2026-04-26 | A fresh session feels invited into the work, not abandoned among evidence fragments. | Prior handoff pattern lives under `outbox/`; helper docs route through `docs/README.md`. | Not started. | Wait until a long run is ending, context is tight, or a checkpoint is about to be handed to another session. Then write a self-contained handoff naming changed files, verified commands, remaining uncertainty, and the exact first reads for the next session. |
| BASELINE-001 | `REFERENCE` | Repo state and build-route baseline | `COMPLETE` | 2026-04-26 | Claims feel earned because source, build, and audit reality come before design confidence. | Captured `git status --short --branch`; inspected dirty diff for Red boss-tier/items plus helper-doc/audit/index wiring; confirmed WSL `make` route with `pokegold.gbc` and `pokesilver.gbc` current. | None for this baseline pass. | Keep as reference. Future gameplay claims should still name whether they came from source/audit/build or emulator play. |
| ROADMAP-001 | `REFERENCE` | Roadmap and helper-doc navigation setup | `COMPLETE` | 2026-04-26 | Future helpers feel oriented fast enough to protect the project instead of reinterpreting it. | `docs/project_roadmap.md`, `docs/README.md`, `docs/project_map.md`, `tools/audit/check_docs_navigation.py` | None. | Keep this as a routing board, not a long implementation diary. |
