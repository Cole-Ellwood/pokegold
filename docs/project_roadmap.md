# Current Project Roadmap

## Boss AI Cognition Mode

For Boss AI work, the roadmap should not domesticate the thinking. Record the
wildest fair ideas, the scary boss-feel questions, and the next deep branch, but
keep implementation rows honest about proof, memory, and no-cheat boundaries.

This is the broad work board for future Codex/helper sessions. Use it after
`docs/codex_context.md` and `docs/project_map.md` when deciding what the current
project is and what still needs attention.

Execution checklist for the current proof/feel sweep:
`docs/project_completion_todo.md`. Use that file for step-by-step todo status,
including `DONE_NEEDS_DOUBLE_CHECK` before promoting a task to fully checked.

Update rule: every session that touches a listed workstream must update its row
when work is completed, started, blocked, deliberately left untouched, or when a
useful idea appears. Keep ideas in `Ideas / next moves`; do not invent new status
values for speculation.

Status legend:

- `UNTOUCHED`: intentionally not started yet.
- `IN_PROGRESS`: started, but not fully verified or finished.
- `STUCK`: blocked by missing input, missing tooling, unclear source truth, or a
  required manual/runtime step.
- `COMPLETE`: finished and verified enough for the evidence listed.

Source-truth rule: this roadmap is an index of current work, not an authority
over source. If it conflicts with source files, linker outputs, or generated
audits, trust the source truth and update this file.

Felt-promise rule: every workstream must name the experience it protects. Beauty
is first evidence that the underlying order is right, then a force that makes
future helpers trust, linger, and protect the work. If a change is technically
correct but makes the hack feel cheap, arbitrary, noisy, dead, or less like fair
hard Pokemon Gold, treat that as a real defect and record it here.

First-Playthrough Promise: roadmap work protects the feeling of being small in
Johto again. The hack should restore uncertainty for a veteran player: bosses
can beat you without cheating, forgotten Pokemon can surprise you, and QoL
respects the journey instead of dissolving it.

Urgency order is top-to-bottom: `DO NOW`, `NEXT`, `SOON`, `WAIT`,
`REFERENCE`. Urgency is separate from status. A complete row can still be
important, but it belongs lower once it is no longer the next decision.

| ID | Urgency | Workstream | Status | Last update | Protected promise | Evidence | Stuck on | Ideas / next moves |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MEGAURGENT-001 | `DO NOW` | First real boss-fight proof capsule | `COMPLETE` | 2026-04-27 | The hack should not call itself fair-hard from static audits alone; at least one current trace ROM boss fight needs live decision proof. | Morty has current live move-decision proof from the real-script state factory: `.local/tmp/boss_state_factory/morty_chosen_frame_5646.state` -> `audit/boss_ai_trace/morty_live.txt` with `chosen_id=101`, `top_moves=CURSE:20,NIGHT_SHADE:20,HYPNOSIS:37`, `plan_id=2`, and matching trace hashes. The manifest pins trace ROM hash `40EF63CE2AE4DCAE791A9417CDC996B1C4A153ACA6E2B7FC101FC58DA22508EE` and trace symbol hash `A79D709AADBAD64D8E57CD81EBAD6D692C9FA70BC9271FA33702498CBD480E5A`, and Morty keeps `preflight.expect = morty`. | None for the first proof capsule. Broader validation is tracked under BOSSAI-001. | Keep the bad frame-161 and delta-263 states out of manifest proof. Use the factory route for future trainer state regeneration instead of hand-stitching battle RAM. |
| VERIFY-001 | `DO NOW` | Verify, build, document, checkpoint workflow | `IN_PROGRESS` | 2026-04-27 | Stopping points feel trustworthy: the next helper can tell what was proven, what merely compiled, and what still needs play. | Existing release-confidence/audit/build passes still stand. Follow-up live proof added `tools/trace/boss_ai_state_factory.py`; `python tools\trace\boss_ai_state_factory.py --all --update-manifest` generated real map/script-created decision states for all 16 gym leaders, Koga, and Champion Lance; `python tools\trace\boss_ai_trace_batch.py --execute` wrote all matching trainer `*_live.txt` files. 2026-04-27 switch-loop pass narrowed the anti-loop emergency exception, rebuilt normal Gold/Silver plus trace ROM, refreshed `docs/generated/dev_index.md`, added `tools/trace/boss_ai_shared_switch_loop_fixture.py`, and captured `audit/boss_ai_trace/shared_switch_loop_live.txt` with manifest-matching trace hashes. 2026-04-27 focused PyBoy feel pass recorded Repel accept/decline, LANTERN acquisition/backfill, SKY_PASS registered use, Earl notebook rendering, and one Falkner loss note in `audit/manual_feel_checks_2026-04-27.md`. | Broad manual playthrough remains separate; the focused pass is not route, full-HM, or curve proof. | Use `audit/release_confidence_2026-04-26.md`, `audit/boss_ai_trace/live_capture_ledger.md`, `audit/boss_ai_trace/shared_switch_loop_live.txt`, `audit/manual_feel_checks_2026-04-27.md`, and `docs/project_completion_todo.md` as the current proof surfaces. |
| NAV-001 | `REFERENCE` | Agent navigation beauty pass | `COMPLETE` | 2026-04-26 | Future sessions find the correct work surface in constant time, then spend their attention on the actual problem instead of rediscovering the repo. | Added `docs/agent_navigation/` with one-screen start card, navigation health check with smoke routes, doc-role ownership map, task routing, source/generated/output ownership policy, verification matrix, artifact catalog, custom terms glossary, entrypoint links, and subsystem micro-indexes for Boss AI trace, trainer/boss rosters, Pokemon balance, QoL/map scripts, and checkpoint/handoff flow. Follow-up simplification folded the fresh-session dry run into `docs/agent_navigation/navigation_health_check.md`, merged path classification into `docs/agent_navigation/source_output_ownership.md`, removed the duplicated subsystem README, and trimmed repeated micro-index verification command blocks. 2026-04-26 docs-only jump pass added a cold jump packet, fast jump mode, one-command navigation floor, dirty-worktree docs-only protocol, and smoke route for future-agent organization prompts. `tools/audit/check_docs_navigation.py` now auto-discovers `docs/agent_navigation/**/*.md` and validates navigation path references, Python command paths, planned missing live-capture artifacts, and core contract snippets. | None for the core navigation layer. This was docs/tooling-only and did not reorganize source files, generated outputs, or ROM behavior. | Keep as reference. Reopen only if a future prompt shape cannot route through `docs/agent_navigation/start_card.md`, `docs/agent_navigation/task_router.md`, or a subsystem micro-index before broad source search. |
| POLISH-001 | `REFERENCE` | 10/10 repo polish and workspace hygiene | `COMPLETE` | 2026-04-26 | The repo feels physically calm without lying about ROM-hack reality: raw folder browsing should explain itself, not hide source truth or break old build assumptions. | 2026-04-26 polish pass followed `outbox/repo_10_10_polish_prompt.md`: required docs were read, tracked source was clean, `git clean -ndX` found 4247 ignored candidates, `git check-ignore -v` confirmed representative output ownership, and `rg` showed root `.gbc`/`.map`/`.sym` assumptions in docs/tools/scripts/Makefile. Added `tools/audit/check_workspace_hygiene.py`, clearer `.gitignore` ownership comments, and navigation/docs pointers so ignored graphics/RGBDS/build/scratch noise is classified before cleanup. Follow-up organization moved root QoL research to `docs/qol_research_report.md`, deleted a stale ignored sprint12 workspace patch after inspection, and documented `.local/` / `dist/` ownership without touching outputs. | None for docs/tooling hygiene. Physical clutter remains intentionally because root linker outputs, trace/debug outputs, `.local/` probes, tracked release artifacts, and old-school generated graphics are explainable and no output relocation was approved. | Keep as reference. Reopen only with a concrete cleanup/output-relocation plan naming exact files, rebuild commands, dev-index/release-smoke impact, restore path, and user approval before destructive commands. |
| VISION-001 | `REFERENCE` | First-playthrough promise wording | `COMPLETE` | 2026-04-26 | Future sessions remember the deep goal: not generic hard mode, but Pokemon Gold made unknown and scary again for a veteran player. | Core wording added to `docs/README.md`, `docs/codex_context.md`, `docs/project_map.md`, Boss AI, balance, QoL, review, authoring, and agent-navigation docs. | None for docs-only intent propagation. | Keep as reference. Reopen if a future doc or implementation plan frames the hack mainly as difficulty, competitive optimization, or modernization instead of restored uncertainty. |
| BOSSAI-001 | `NEXT` | Boss AI design-promise validation | `IN_PROGRESS` | 2026-04-27 | Bosses feel like strong legal opponents: pressure without clairvoyance, and surprises that make sense afterward. | Static Boss AI/no-cheat/gating/trace/memory/leader-wiring audits pass, and all 16 gym leaders plus Koga and Champion Lance have current trace-ROM first-decision live captures under `audit/boss_ai_trace/*_live.txt` from `.local/tmp/boss_state_factory/*_chosen_frame_*.state`. The shared switch-loop fixture now proves the A->B->A penalty from a synthetic Jasmine battle context: `switch_confidence=80`, `switch_context=param=31,index=00,last_out=02,cooldown=02,cur_ot=00`, normal threshold `74`, anti-loop threshold `84`. The source now keeps generic public type pressure from waiving that penalty while preserving low-HP, public revenge, Perish Song, immunity-pivot, and ace-timing exceptions. Haki remains a future once-per-battle pure-cheat exception, not implemented source behavior. | Red is not currently in the manifest. First-decision captures prove live execution and chosen moves, not every design-note branch in every matchup. Haki has a design contract but no source implementation. Manual boss-feel playtesting remains separate from trace proof. | Next live proof should add Red only if it becomes a real manifest target, or create richer per-boss scenario states for specific behaviors like phazing and public fail gates. |
| DIFFICULTY-001 | `NEXT` | Cheap-difficulty audit | `IN_PROGRESS` | 2026-04-27 | Losses feel deserved and instructive, not like the designer punished the player for trusting the game. | Project intent in `docs/codex_context.md`; review route in `docs/codex_review_playbook.md`. 2026-04-26 static pass added `tools/audit/check_cheap_difficulty.py`, routed it through agent navigation, and recorded `audit/cheap_difficulty_2026-04-26.md`. The audit passed on 43 target entries / 206 mons, checking early-tier late-item leakage, Choice item timing, Life Orb / Assault Vest timing, Light Ball ownership, party-size pressure by tier, Rival starter-branch parity, and Johto Gym peak-level curve `11, 17, 21, 26, 32, 34, 33, 39`. Adjacent `bug_hunt_triage`, AI tier, boss item, and boss move audits were quiet/passing. 2026-04-27 focused PyBoy note lost to Falkner from a synthetic underleveled state: level 11 Totodile into level 19 Pidgeotto. The loss reason was visible, but this is not curve proof. | Static cheapness tripwire exists, and one loss-feel note exists; representative route/playthrough difficulty still needs manual fight review. | Run `python tools\audit\check_cheap_difficulty.py` after trainer-party or itemization edits. Next deeper pass should review a natural-route loss and ask whether the player could infer the answer before losing. |
| BALANCE-001 | `NEXT` | Weak-Pokemon usefulness queue | `IN_PROGRESS` | 2026-04-27 | Forgotten Pokemon should create discovery pressure, not remain solved jokes with bigger numbers. | 2026-04-26 Delibird pass upgraded the source learnset from `PRESENT` only to Present, Quick Attack, Icy Wind, Aurora Beam, Agility, Wing Attack, Haze, and Blizzard while leaving stats/type/encounters untouched. 2026-04-27 Farfetch'd pass resolved the selected lane from `audit/forward_improvement_lane_2026-04-27.md`: wild Farfetch'd now commonly carry `STICK`, learn `WING_ATTACK` at level 23, and the `STICK` item text names its critical-ratio effect. `docs/balance_intent.md` records Farfetch'd as a provisional Stick-backed midgame crit attacker, `docs/buff_backlog.md` moves it to resolved, and `docs/generated/balance_audit.md` now shows `WING_ATTACK` as its best reliable STAB. 2026-04-27 learnset-order cleanup restored increasing-level order for 24 level-up rows and added a release-smoke guard, fixing generated move ordering without changing any move levels. | Manual Silver Ice Path Delibird catch feel, PokefanM Colin fight feel, Route 38 Farfetch'd catch feel, and Bird Keeper Jose/Perry Farfetch'd showcases were not run. `ARIADOS` and `YANMA` remain in the broader queue. | Next weak-Pokemon source pass should pick exactly one of `ARIADOS` or `YANMA`, then inspect source truth and either give it a real role or document an intentional niche. Do not start another broad weak-Pokemon batch. |
| QOL-001 | `REFERENCE` | QoL weirdness and friction pass | `COMPLETE` | 2026-04-27 | Friction reduction feels like respect for the player's time and hands without erasing preparation or Gen 2 texture. | `docs/qol_handoff.md` tracks implemented QoL and remaining candidates; `docs/qol_research_report.md` holds the longer research rationale for keeping decision costs while killing clerical costs. 2026-04-26 text-only pass clarified TM Voucher / held-item descriptions and Route 34 / Day-Care service discovery without adding events, flags, or economy changes. 2026-04-26 Pokemon Center pass shortened only fixed healing pauses while preserving heal prompt, `HealParty`, `HealMachineAnim`, music restart, Pokerus check, and nurse turns. 2026-04-26 Repel renewal pass prompts only when another Repel is in Bag outside Bug-Catching Contest, prioritizes Max/Super/regular, consumes exactly one item, and writes the normal step count without new memory. 2026-04-26 HM-tool pass added seven key items for field use, kept acquisition/badge gates, converted old HM discs into consumable/forgettable TM51-TM57, and lets legacy HM reward flags backfill missing tools for old saves. 2026-04-26 TM Tutor bugfix keeps selected `wCurItem` across `TeachTMHM` via stack restore; release smoke guards it. 2026-04-27 PyBoy check confirmed Repel decline/accept feel, LANTERN acquisition/backfill, and registered SKY_PASS use; it also noted generic `TMs` receive/put-away wording for the converted disc as a minor polish rough edge. | Source flow and build/audits are complete. Focused manual checks exist, but every HM replacement item, Bag path, obstacle path, and near-capacity pocket was not exhaustively played. | Keep as reference. Reopen only for the noted converted-disc text polish, a concrete Repel/HM-tool bug report, or a new QoL candidate outside this handoff. |
| COMM-001 | `REFERENCE` | Early player communication for fair-hard rules | `COMPLETE` | 2026-04-27 | The player should understand why the game is stricter before a fair rule feels like a cheap surprise. | Earl's Academy notebook in `maps/EarlsPokemonAcademy.asm` now teaches Set/no-Pack trainer battles, Gym Leaders remembering revealed public moves, stronger type habits, and serious held-item boost/lock behavior. `tools/audit/check_release_smoke.py` guards the notebook text. `audit/player_communication_2026-04-26.md` records the lane and checks. WSL normal Gold/Silver build passed after relinking map data; `python scripts\generate_dev_index.py --rom pokegold` refreshed `docs/generated/dev_index.md`; final release smoke, docs navigation, and `git diff --check` passed. 2026-04-27 PyBoy screenshots in `audit/manual_feel_checks_2026-04-27.md` confirm the notebook text renders without visible overflow. | No source blocker. Broader first-playthrough comprehension still depends on natural playtesting. | If reopening player communication, first look for a concrete blind spot. Avoid stuffing every mechanic into text; use one early Academy surface plus item/NPC text unless playtest evidence says another surface is needed. |
| IMPROVE-001 | `REFERENCE` | Broad important-improvement loop | `COMPLETE` | 2026-04-26 | Vague "make it better" sessions should improve the game or the repo in a way that compounds, not scatter attention across cosmetic busywork. | `docs/agent_navigation/important_improvement_menu.md` now holds the full productive-lane map for broad improvement prompts, including boss feel, live proof, cheap difficulty, weak Pokemon, QoL, battle correctness, release confidence, docs, hygiene, playtest artifacts, and bold prototype work. `docs/agent_navigation/start_card.md`, `docs/agent_navigation/README.md`, and `docs/agent_navigation/task_router.md` route broad "make the app/game better" prompts into that menu before lane selection. `outbox/make_app_better_prompt.md` is the spam-ready prompt for fresh sessions. | None for the routing/prompt surface. Each actual improvement session must still choose one lane and verify against that lane's row. | When using the loop, prefer trust before novelty: live proof, cheap-difficulty evidence, release confidence, or sharp future-agent notes usually compound better than a random feature. Keep constant notes in `outbox/`, `audit/`, and this roadmap according to durability. |
| BOSSAI-002 | `WAIT` | Gym scout dossier system | `UNTOUCHED` | 2026-04-26 | Gym leaders feel prepared because the world watched the player, not because the ROM cheated. | Design idea from 2026-04-26 thinking pass: every gym trainer can report only visible player battle facts to that gym's leader, and the player should be able to inspect the resulting dossier before the boss. | Not started; needs a tiny Morty-only prototype before committing to all gyms. | Wait until existing Boss AI has live proof. Then prototype in Ecruteak only: record coarse public facts from gym-trainer battles, show the report through gym-guide or leader-adjacent text before Morty, and seed Morty's boss AI with only those public facts. Expand only if it feels fair, tense, and useful for hidden weak-Pokemon utility. |
| HANDOFF-001 | `WAIT` | Final durable handoff artifact | `UNTOUCHED` | 2026-04-26 | A fresh session feels invited into the work, not abandoned among evidence fragments. | Prior handoff pattern lives under `outbox/`; helper docs route through `docs/README.md`. | Not started. | Wait until a long run is ending, context is tight, or a checkpoint is about to be handed to another session. Then write a self-contained handoff naming changed files, verified commands, remaining uncertainty, and the exact first reads for the next session. |
| BASELINE-001 | `REFERENCE` | Repo state and build-route baseline | `COMPLETE` | 2026-04-26 | Claims feel earned because source, build, and audit reality come before design confidence. | Captured `git status --short --branch`; inspected dirty diff for Red boss-tier/items plus helper-doc/audit/index wiring; confirmed WSL `make` route with `pokegold.gbc` and `pokesilver.gbc` current. | None for this baseline pass. | Keep as reference. Future gameplay claims should still name whether they came from source/audit/build or emulator play. |
| ROADMAP-001 | `REFERENCE` | Roadmap and helper-doc navigation setup | `COMPLETE` | 2026-04-26 | Future helpers feel oriented fast enough to protect the project instead of reinterpreting it. | `docs/project_roadmap.md`, `docs/README.md`, `docs/project_map.md`, `tools/audit/check_docs_navigation.py` | None. | Keep this as a routing board, not a long implementation diary. |
