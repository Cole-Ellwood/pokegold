# Current Project Roadmap

## Boss AI Cognition Mode

For Boss AI work, the roadmap should not domesticate the thinking. Record the
wildest fair ideas, the scary boss-feel questions, and the next deep branch, but
keep implementation rows honest about proof, memory, and no-cheat boundaries.

This is the broad work board for future Codex/helper sessions. Use it after
`docs/project_context.md` and `docs/project_map.md` when deciding what the current
project is and what still needs attention.

Execution checklist for the current proof/feel sweep:
`docs/project_completion_todo.md`. Use that file for step-by-step todo status,
including `DONE_NEEDS_DOUBLE_CHECK` before promoting a task to fully checked.

Update rule: every session that touches a listed workstream must update its row
when work is completed, started, blocked, deliberately left untouched, or when a
useful idea appears. Keep ideas in `Ideas / next moves`; do not invent new status
values for speculation.

Latest DEBUGGER-001 status note (2026-05-24): symptom-only
`python -m tools.debugger investigate --symptom ...` now remains a planning
packet, but embeds the full `unified_debugger_next_step` route so downstream
`rank`, `report`, and `visualize` preserve first proof command, source/data
anchors, evidence/disproof standards, and regression gate. Golden/lived
scenario: `boss selected wrong switch` routes to `rom-switch-materialize`.
Follow-up in the same slice: `localize --report` now consumes that embedded
next-step route, promotes the source/data anchors into localization candidates,
and carries the routed proof/escalation/regression commands into the workflow
plan.
Next follow-up: `explain --report` now turns the embedded next-step route into a
causal proof-path chain with first command, source/data anchors,
evidence/disproof standards, proof limit, and regression gate.
Latest follow-up: `suggest-tests --report` now consumes the same embedded route
and turns its first proof command, regression gate, escalation command,
required inputs, source/data anchors, evidence/disproof standards, and proof
limit into test/regression guidance.
Replay follow-up: `replay --report` now consumes embedded next-step routes as
concrete replay targets and stops promoting impact-report prose tags such as
`Broad`, `Fuzz`, and `ROM-materialized` into `--symbol` arguments.
Setup follow-up: `setup --report` now treats an embedded next-step route as the
setup surface/target source of truth, keeping wrong-switch setup on Boss AI
instead of inheriting nested damage or banking setup lanes from generated
subreports.
Generation/fuzz follow-up: `generate --report` and `fuzz --report` now treat an
embedded next-step route as the generator surface/family source of truth; the
wrong-switch route stays on Boss AI `switch_sack` generation and
`rom-switch-materialize --fail-on-mismatch`. Focused regression:
`test_generate_uses_embedded_next_step_instead_of_nested_report_surfaces`.
Compare follow-up: `compare --report` now treats an embedded next-step route as
the mirror lane source of truth; the wrong-switch route selects the Boss AI
mirror and carries `rom-switch-materialize --fail-on-mismatch` instead of
falling back to an uncovered generic surface. Focused regression:
`test_compare_plan_uses_embedded_next_step_as_mirror_route`.

Status legend:

- `UNTOUCHED`: intentionally not started yet.
- `IN_PROGRESS`: started, but not fully verified or finished.
- `STUCK`: blocked by missing input, missing tooling, unclear source truth, or a
  required manual/runtime step.
- `COMPLETE`: finished and verified enough for the evidence listed.

Source-truth rule: this roadmap is an index of current work, not an authority
over source. If it conflicts with source files, linker outputs, or generated
audits, trust the source truth and update this file.

Felt-promise rule: every workstream must name the experience it protects. Beauty
is first evidence that the underlying order is right, then a force that makes
future helpers trust, linger, and protect the work. If a change is technically
correct but makes the hack feel cheap, arbitrary, noisy, dead, or less like fair
hard Pokemon Gold, treat that as a real defect and record it here.

First-Playthrough Promise: roadmap work protects the feeling of being small in
Johto again. The hack should restore uncertainty for a veteran player: bosses
can beat you without cheating, forgotten Pokemon can surprise you, and QoL
respects the journey instead of dissolving it.

Urgency order is top-to-bottom: `DO NOW`, `NEXT`, `SOON`, `WAIT`,
`REFERENCE`. Urgency is separate from status. A complete row can still be
important, but it belongs lower once it is no longer the next decision.

| ID | Urgency | Workstream | Status | Last update | Protected promise | Evidence | Stuck on | Ideas / next moves |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MEGAURGENT-001 | `DO NOW` | First real boss-fight proof capsule | `COMPLETE` | 2026-05-14 | The hack should not call itself fair-hard from static audits alone; at least one current trace ROM boss fight needs live decision proof. | Morty has current live move-decision proof from the real-script state factory: `.local/tmp/boss_state_factory/morty_chosen_frame_5422.state` -> `audit/boss_ai_trace/morty_live.txt` with `chosen_id=174`, `top_moves=HYPNOSIS:36,CURSE:37,NIGHT_SHADE:37`, `plan_id=2`, and matching trace hashes. The manifest pins trace ROM hash `27602A6578987D77EFD4CCF70AB2D323AA6D6A2DED211602E016A2AAB2062447` and trace symbol hash `53CAF507E51D31C5A958A739ABD1A8097483B02DC9EB27750C28266746465D82`, and Morty keeps `preflight.expect = morty`. | None for the first proof capsule. Broader validation is tracked under BOSSAI-001. | Keep the bad frame-161 and delta-263 states out of manifest proof. Use the factory route for future trainer state regeneration instead of hand-stitching battle RAM. |
| VERIFY-001 | `DO NOW` | Verify, build, document, checkpoint workflow | `IN_PROGRESS` | 2026-05-04 | Stopping points feel trustworthy: the next helper can tell what was proven, what merely compiled, and what still needs play. | Existing release-confidence/audit/build passes still stand. Follow-up live proof added `tools/trace/boss_ai_state_factory.py`; `python tools\trace\boss_ai_state_factory.py --all --update-manifest` generated real map/script-created decision states for all 16 gym leaders, Koga, and Champion Lance; `python tools\trace\boss_ai_trace_batch.py --execute` wrote all matching trainer `*_live.txt` files. 2026-04-27 switch-loop pass narrowed the anti-loop emergency exception, rebuilt normal Gold/Silver plus trace ROM, refreshed `docs/generated/dev_index.md`, added `tools/trace/boss_ai_shared_switch_loop_fixture.py`, and captured `audit/boss_ai_trace/shared_switch_loop_live.txt` with manifest-matching trace hashes. 2026-04-27 focused PyBoy feel pass recorded Repel accept/decline, LANTERN acquisition/backfill, SKY_PASS registered use, Earl notebook rendering, and one Falkner loss note in `audit/manual_feel_checks_2026-04-27.md`. 2026-04-27 release-smoke pass now guards the seven documented Gold/Silver bug fixes from `docs/bugs_and_glitches.md`. 2026-04-27 checklist follow-up makes the source-change order explicit: build, regenerate `docs/generated/dev_index.md` if linker outputs changed and are kept, then run docs navigation; a follow-up clarified docs navigation remains an always-run final check. 2026-05-04 verification-floor stays green on the `claude/romantic-meitner-314fb1` work: the AG-NN asm-guide audit pass and TD-### tech-debt track shipped a wave of audits and fixes (see TECHDEBT-001 row for the full delivery list); `tools/verify_sha1.py` PASS for all 6 outputs; `tools/audit/check_release_smoke.py` PASS modulo the pre-existing stale-claims FAILs that resolve on branch merge into `codex/cleanup-gsc-rebalance-split`. | Broad manual playthrough remains separate; the focused pass is not route, full-HM, or curve proof. | Use `audit/release_confidence_2026-04-26.md`, `audit/boss_ai_trace/live_capture_ledger.md`, `audit/boss_ai_trace/shared_switch_loop_live.txt`, `audit/manual_feel_checks_2026-04-27.md`, and `docs/project_completion_todo.md` as the current proof surfaces. |
| DEBUGGER-001 | `DO NOW` | Unified debugger as repo-wide investigation front door | `IN_PROGRESS` | 2026-05-24 | A future helper can ask a ROM-hack question and get the next proof command, required inputs, proof boundary, source/data anchors, evidence/disproof standards, and regression gate without losing that context during report handoff. | 2026-05-24 slice made `unified_debugger_next_step` JSON a first-class `report`/`rank` input. Golden scenario: `python -m tools.debugger next --symptom "boss selected wrong switch" --json-out .local\tmp\debugger_next_wrong_switch.json` followed by `python -m tools.debugger report --report .local\tmp\debugger_next_wrong_switch.json --out .local\tmp\debugger_next_wrong_switch.md`; the rendered report now preserves `rom-switch-materialize`, the scenario JSONL requirement, proof limit, and escalation instead of showing `Unsupported report kind`. Follow-up slice made `unified_debugger_capability_report` JSON first-class too: `python -m tools.debugger audit --json-out .local\tmp\debugger_audit_current.json` followed by `python -m tools.debugger report --report .local\tmp\debugger_audit_current.json --out .local\tmp\debugger_audit_report.md` now renders six `capability_partial` findings with readiness counts and next proof commands. Visualization follow-up made both JSON kinds first-class in `visualize`: `python -m tools.debugger visualize --report .local\tmp\debugger_next_wrong_switch.json --out .local\tmp\debugger_next_visualization.md --json-out .local\tmp\debugger_next_visualization.json` and `python -m tools.debugger visualize --report .local\tmp\debugger_audit_current.json --out .local\tmp\debugger_audit_visualization.md --json-out .local\tmp\debugger_audit_visualization.json` now produce proof-route/readiness timeline, waterfall, and graph entries instead of empty visualizations. Regression-gate follow-up made `next` rows carry `regression_gate` through CLI, JSON, `rank`, `report`, and `visualize`; the wrong-switch scenario now names `rom-switch-materialize` as both proof route and regression gate, and `python tools\audit\check_debugger_next_coverage.py` enforces the field. Source-anchor follow-up made `next` rows carry real source/data paths through CLI, JSON, `rank`, `report`, and `visualize`; the wrong-switch route now points to `tools/boss_ai_debugger/rom_switch_materialize.py`, `engine/battle/ai/boss_policy_switch.asm`, `engine/battle/ai/switch.asm`, and the Boss AI debugger docs, with `python tools\audit\check_debugger_next_coverage.py` rejecting stale paths. Evidence-standard follow-up made `next` rows state the positive condition for evidence strong enough to trust, carried through CLI, JSON, `rank`, `report`, and `visualize`; wrong-switch now requires a matching scenario JSONL passing `rom-switch-materialize --fail-on-mismatch` on the current ROM. Disproof-standard follow-up made `next` rows also state the condition that should reject a route or bug claim; wrong-switch is disproven for a scenario when matching JSONL passes `rom-switch-materialize` with the expected switch result. `suggest-tests --report` follow-up now consumes embedded next-step routes from investigation packets and promotes their first proof command, regression gate, escalation command, required inputs, source/data anchors, evidence/disproof standards, and proof limit into test/regression guidance. `replay --report` follow-up now consumes the same embedded route as concrete replay targets and blocks impact-report prose tags from becoming symbol arguments; wrong-switch replay now targets `BossAI_SwitchOrTryItem`, `wEnemySwitchMonIndex`, `wEnemySwitchMonParam`, and `wEnemyAIMoveScores`. `setup --report` follow-up now treats embedded next-step routes as the setup surface/target source of truth, so wrong-switch setup stays on Boss AI instead of inheriting nested damage or banking setup lanes. Focused regressions: `test_static_report_preserves_next_step_proof_path`, `test_static_report_preserves_capability_audit_gaps`, `test_visualization_preserves_next_step_proof_path`, `test_visualization_preserves_capability_audit_gaps`, `test_suggest_tests_uses_embedded_next_step_regression_gate_from_investigation_report`, `test_replay_plan_uses_embedded_next_step_instead_of_impact_tags`, and `test_setup_plan_uses_embedded_next_step_instead_of_nested_report_surfaces`. | `python -m tools.debugger audit` still reports `ready=False` with partial whole-ROM replay/localization, causal provenance, generation/fuzzing, mirrors, impact workflow, and visualization/report gaps. | Next slices should keep improving existing routing/report/proof surfaces. Favor affordances that turn planned evidence into a reproducible or disprovable proof path, then close each slice with one lived workflow smoke plus the relevant audit/test gate. |
| NAV-001 | `REFERENCE` | Agent navigation beauty pass | `COMPLETE` | 2026-04-26 | Future sessions find the correct work surface in constant time, then spend their attention on the actual problem instead of rediscovering the repo. | Added `docs/agent_navigation/` with one-screen start card, navigation health check with smoke routes, doc-role ownership map, task routing, source/generated/output ownership policy, verification matrix, artifact catalog, custom terms glossary, entrypoint links, and subsystem micro-indexes for Boss AI trace, trainer/boss rosters, Pokemon balance, QoL/map scripts, and checkpoint/handoff flow. Follow-up simplification folded the fresh-session dry run into `docs/agent_navigation/navigation_health_check.md`, merged path classification into `docs/agent_navigation/source_output_ownership.md`, removed the duplicated subsystem README, and trimmed repeated micro-index verification command blocks. 2026-04-26 docs-only jump pass added a cold jump packet, fast jump mode, one-command navigation floor, dirty-worktree docs-only protocol, and smoke route for future-agent organization prompts. `tools/audit/check_docs_navigation.py` now auto-discovers `docs/agent_navigation/**/*.md` and validates navigation path references, Python command paths, planned missing live-capture artifacts, and core contract snippets. | None for the core navigation layer. This was docs/tooling-only and did not reorganize source files, generated outputs, or ROM behavior. | Keep as reference. Reopen only if a future prompt shape cannot route through `docs/agent_navigation/start_card.md`, `docs/agent_navigation/task_router.md`, or a subsystem micro-index before broad source search. |
| POLISH-001 | `REFERENCE` | 10/10 repo polish and workspace hygiene | `COMPLETE` | 2026-04-26 | The repo feels physically calm without lying about ROM-hack reality: raw folder browsing should explain itself, not hide source truth or break old build assumptions. | 2026-04-26 polish pass: required docs were read, tracked source was clean, `git clean -ndX` found 4247 ignored candidates, `git check-ignore -v` confirmed representative output ownership, and `rg` showed root `.gbc`/`.map`/`.sym` assumptions in docs/tools/scripts/Makefile. Added `tools/audit/check_workspace_hygiene.py`, clearer `.gitignore` ownership comments, and navigation/docs pointers so ignored graphics/RGBDS/build/scratch noise is classified before cleanup. Follow-up organization moved root QoL research to `docs/qol_research_report.md`, deleted a stale ignored sprint12 workspace patch after inspection, and documented `.local/` / `dist/` ownership without touching outputs. | None for docs/tooling hygiene. Physical clutter remains intentionally because root linker outputs, trace/debug outputs, `.local/` probes, tracked release artifacts, and old-school generated graphics are explainable and no output relocation was approved. | Keep as reference. Reopen only with a concrete cleanup/output-relocation plan naming exact files, rebuild commands, dev-index/release-smoke impact, restore path, and user approval before destructive commands. |
| VISION-001 | `REFERENCE` | First-playthrough promise wording | `COMPLETE` | 2026-04-26 | Future sessions remember the deep goal: not generic hard mode, but Pokemon Gold made unknown and scary again for a veteran player. | Core wording added to `docs/README.md`, `docs/project_context.md`, `docs/project_map.md`, Boss AI, balance, QoL, review, authoring, and agent-navigation docs. | None for docs-only intent propagation. | Keep as reference. Reopen if a future doc or implementation plan frames the hack mainly as difficulty, competitive optimization, or modernization instead of restored uncertainty. |
| BOSSAI-001 | `NEXT` | Boss AI design-promise validation | `IN_PROGRESS` | 2026-04-27 | Bosses feel like strong legal opponents: pressure without clairvoyance, and surprises that make sense afterward. | Static Boss AI/no-cheat/gating/trace/memory/leader-wiring audits pass, and all 16 gym leaders plus Koga and Champion Lance have current trace-ROM first-decision live captures under `audit/boss_ai_trace/*_live.txt` from `.local/tmp/boss_state_factory/*_chosen_frame_*.state`. The shared switch-loop fixture records the current synthetic Jasmine repeated-switch context with `switch_confidence=90` and `switch_context=param=31,index=00,last_out=02,cooldown=02,cur_ot=00`; it is current trace coverage, not a preserved threshold proof. Haki remains a future once-per-battle pure-cheat exception, not implemented source behavior. | Red is not currently in the manifest. First-decision captures prove live execution and chosen moves, not every design-note branch in every matchup. Haki has a design contract but no source implementation. Manual boss-feel playtesting remains separate from trace proof. | Next live proof should add Red only if it becomes a real manifest target, or create richer per-boss scenario states for specific behaviors like phazing and public fail gates. |
| DIFFICULTY-001 | `NEXT` | Cheap-difficulty audit | `IN_PROGRESS` | 2026-04-27 | Losses feel deserved and instructive, not like the designer punished the player for trusting the game. | Project intent in `docs/project_context.md`; review route in `docs/review_playbook.md`. 2026-04-26 static pass added `tools/audit/check_cheap_difficulty.py`, routed it through agent navigation, and recorded `audit/cheap_difficulty_2026-04-26.md`. The audit passed on 43 target entries / 206 mons, checking early-tier late-item leakage, Choice item timing, Life Orb / Assault Vest timing, Light Ball ownership, party-size pressure by tier, Rival starter-branch parity, and Johto Gym peak-level curve `11, 17, 21, 26, 32, 34, 33, 39`. Adjacent `bug_hunt_triage`, AI tier, boss item, and boss move audits were quiet/passing. 2026-04-27 focused PyBoy note lost to Falkner from a synthetic underleveled state: level 11 Totodile into level 19 Pidgeotto. The loss reason was visible, but this is not curve proof. | Static cheapness tripwire exists, and one loss-feel note exists; representative route/playthrough difficulty still needs manual fight review. | Run `python tools\audit\check_cheap_difficulty.py` after trainer-party or itemization edits. Next deeper pass should review a natural-route loss and ask whether the player could infer the answer before losing. |
| BALANCE-001 | `NEXT` | Weak-Pokemon usefulness queue | `IN_PROGRESS` | 2026-04-27 | Forgotten Pokemon should create discovery pressure, not remain solved jokes with bigger numbers. | 2026-04-26 Delibird pass upgraded the source learnset from `PRESENT` only to Present, Quick Attack, Icy Wind, Aurora Beam, Agility, Wing Attack, Haze, and Blizzard while leaving stats/type/encounters untouched. 2026-04-27 Farfetch'd pass resolved the selected lane from `audit/forward_improvement_lane_2026-04-27.md`: wild Farfetch'd now commonly carry `STICK`, learn `WING_ATTACK` at level 23, and the `STICK` item text names its critical-ratio effect. `docs/balance_intent.md` records Farfetch'd as a provisional Stick-backed midgame crit attacker, `docs/buff_backlog.md` moves it to resolved, and `docs/generated/balance_audit.md` now shows `WING_ATTACK` as its best reliable STAB. 2026-04-27 learnset-order cleanup restored increasing-level order for 24 level-up rows and added a release-smoke guard, fixing generated move ordering without changing any move levels. 2026-04-27 Ariados pass made the role explicit as a slow hazard trapper: `SPIKES` at level 22, `SPIDER_WEB` at level 37, release-smoke guard, `audit/ariados_balance_2026-04-27.md`, and provisional intent/backlog rows. 2026-04-27 Yanma pass made the role explicit as a fast physical Bug/Flying disruptor: `LEECH_LIFE` moved from level 19 to 17 so Schoolboy Alan's first Yanma and trained Route 35 catches show Bug STAB before level-25 Wing Attack; see `audit/yanma_balance_2026-04-27.md`. | Manual Silver Ice Path Delibird catch feel, PokefanM Colin fight feel, Route 38 Farfetch'd catch feel, Bird Keeper Jose/Perry Farfetch'd showcases, evolved Spinarak level 22-37 feel, Koga's Ariados trap/hazard feel, Route 35 Yanma catch feel, and Schoolboy Alan Yanma showcase fights were not run. No medium-priority weak-Pokemon source lane is currently active; lower-priority watchlist still needs judgment. | Next weak-Pokemon source pass should not invent a medium-priority queue. If reopening balance, inspect the lower-priority watchlist one species at a time, starting from current source truth and trainer/encounter usage. |
| QOL-001 | `REFERENCE` | QoL weirdness and friction pass | `COMPLETE` | 2026-04-27 | Friction reduction feels like respect for the player's time and hands without erasing preparation or Gen 2 texture. | `docs/qol_handoff.md` tracks implemented QoL and remaining candidates; `docs/qol_research_report.md` holds the longer research rationale for keeping decision costs while killing clerical costs. 2026-04-26 text-only pass clarified then-current lesson-item / held-item descriptions and Route 34 / Day-Care service discovery without adding events, flags, or economy changes; 2026-05-08 removal later retired those lesson-item text surfaces. 2026-04-26 Pokemon Center pass shortened only fixed healing pauses while preserving heal prompt, `HealParty`, `HealMachineAnim`, music restart, Pokerus check, and nurse turns. 2026-04-26 Repel renewal pass prompts only when another Repel is in Bag outside Bug-Catching Contest, prioritizes Max/Super/regular, consumes exactly one item, and writes the normal step count without new memory. 2026-04-26 HM-tool pass added seven key items for field use, kept acquisition/badge gates, converted old HM discs into consumable/forgettable TM51-TM57, and lets legacy HM reward flags backfill missing tools for old saves. 2026-04-26 selected-item restore bugfix applied to the now-retired lesson path. 2026-04-27 PyBoy check confirmed Repel decline/accept feel, LANTERN acquisition/backfill, and registered SKY_PASS use; it also noted generic `TMs` receive/put-away wording for the converted disc as a minor polish rough edge. | Source flow and build/audits are complete. Focused manual checks exist, but every HM replacement item, Bag path, obstacle path, and near-capacity pocket was not exhaustively played. | Keep as reference. Reopen only for the noted converted-disc text polish, a concrete Repel/HM-tool bug report, or a new QoL candidate outside this handoff. |
| COMM-001 | `REFERENCE` | Early player communication for fair-hard rules | `COMPLETE` | 2026-04-27 | The player should understand why the game is stricter before a fair rule feels like a cheap surprise. | Earl's Academy notebook in `maps/EarlsPokemonAcademy.asm` now teaches Set/no-Pack trainer battles, Gym Leaders remembering revealed public moves, stronger type habits, and serious held-item boost/lock behavior. `tools/audit/check_release_smoke.py` guards the notebook text. `audit/player_communication_2026-04-26.md` records the lane and checks. WSL normal Gold/Silver build passed after relinking map data; `python scripts\generate_dev_index.py --rom pokegold` refreshed `docs/generated/dev_index.md`; final release smoke, docs navigation, and `git diff --check` passed. 2026-04-27 PyBoy screenshots in `audit/manual_feel_checks_2026-04-27.md` confirm the notebook text renders without visible overflow. | No source blocker. Broader first-playthrough comprehension still depends on natural playtesting. | If reopening player communication, first look for a concrete blind spot. Avoid stuffing every mechanic into text; use one early Academy surface plus item/NPC text unless playtest evidence says another surface is needed. |
| IMPROVE-001 | `REFERENCE` | Broad important-improvement loop | `COMPLETE` | 2026-04-26 | Vague "make it better" sessions should improve the game or the repo in a way that compounds, not scatter attention across cosmetic busywork. | `docs/agent_navigation/important_improvement_menu.md` now holds the full productive-lane map for broad improvement prompts, including boss feel, live proof, cheap difficulty, weak Pokemon, QoL, battle correctness, release confidence, docs, hygiene, playtest artifacts, and bold prototype work. `docs/agent_navigation/start_card.md`, `docs/agent_navigation/README.md`, and `docs/agent_navigation/task_router.md` route broad "make the app/game better" prompts into that menu before lane selection. | None for the routing/prompt surface. Each actual improvement session must still choose one lane and verify against that lane's row. | When using the loop, prefer trust before novelty: live proof, cheap-difficulty evidence, release confidence, or sharp future-agent notes usually compound better than a random feature. Keep constant notes in `audit/`, `decisions/`, `journal/`, and this roadmap according to durability. |
| BOSSAI-002 | `WAIT` | Gym scout dossier system | `UNTOUCHED` | 2026-04-26 | Gym leaders feel prepared because the world watched the player, not because the ROM cheated. | Design idea from 2026-04-26 thinking pass: every gym trainer can report only visible player battle facts to that gym's leader, and the player should be able to inspect the resulting dossier before the boss. | Not started; needs a tiny Morty-only prototype before committing to all gyms. | Wait until existing Boss AI has live proof. Then prototype in Ecruteak only: record coarse public facts from gym-trainer battles, show the report through gym-guide or leader-adjacent text before Morty, and seed Morty's boss AI with only those public facts. Expand only if it feels fair, tense, and useful for hidden weak-Pokemon utility. |
| BOSSAI-003 | `REFERENCE` | Boss AI policy contract + decision-debugger tool | `COMPLETE` | 2026-05-08 | Bosses make decisions a human watching the screen would call smart, anchored to a stated philosophy a player could read off. Not bigger numbers; better thinking. Pokemon is a hidden-info game (poker, not chess), so bosses must reason from public uncertainty instead of private reads or deterministic habit. | Completed as the minimal source-aware path. Source audit found the current split Boss AI source under `engine/battle/ai/` already contains the accepted simpler architecture: public plausible type masks, plan id/confidence, lookahead for top candidates, weighted best-vs-second move choice, switch prediction, scouting, repeat penalties, and plausible-risk switch refinement. Added `engine/battle/ai/PLATFORM_API.md`, `engine/battle/ai/POLICY_DESIGN.md`, fixture-backed `tools/boss_ai_debugger/`, and `tools/audit/check_boss_ai_policy_contract.py`. BOSSAI-004 labels are now the taste corpus for future behavior changes. | No ROM policy rewrite landed because source evidence did not justify replacing working audited code. Existing behavior remains guarded by no-cheat, gating, trace invariant, memory-budget, index-line, and policy-contract audits. The remaining uncertainty is taste/playtest data: the preference corpus starts empty until the user labels states. | Future Boss AI changes should start by labeling or adding a BOSSAI-004 fixture, inspecting it with `python -m tools.boss_ai_debugger inspect --fixture-id <id>`, and only then editing the split Boss AI source if the public-info explanation is clear. If source changes, rerun the Boss AI audit floor in `PLATFORM_API.md`; do not regenerate live traces blindly. |
| BOSSAI-004 | `REFERENCE` | Boss AI preference-training side app (RLHF-ish lab) | `COMPLETE` | 2026-05-10 | The user can teach the boss AI's taste through concrete public-info battle states without committing to a black-box policy. Labels capture whether a legal action felt best, good, bad, cheap, scary-good, context-dependent, or a calculated risk; the output stays readable and reviewable. | Version A implemented in `tools/boss_ai_preference/`: schema, 20 public-info fixtures, JSONL label persistence, local stdlib web UI, CLI label command, Markdown/JSON preference report, source-derived player threat availability report, and `tools/audit/check_boss_ai_preference.py`. The app now shows relevant incoming player threats with coarse `0/25/50/75/99%` likelihood buckets, rough damage percent, severity, immediacy, and switch-fit checks so bad seen-party switch-ins do not read like active pressure. The threat map parses level-up moves, TM/HM learnsets, wild grass, Surf-gated water, fishing by rod tier, simple `givepoke` gifts/prizes, listed statics, direct TM rewards, and badge-gated map access. The removed Day-Care lesson path is intentionally absent from the current threat model. Default reports live in `audit/boss_ai_preference/latest_report.*` and `audit/boss_ai_preference/threat_availability_report.*`. V2 planning now lives in `docs/boss_ai_rlhf_v2_implementation_plan.md`: active query queue, structured lesson records, counterfactual fixture families, offline readable preference model, and proposal reports. | No ROM policy rewrite landed; this is side-app tooling and corpus collection. Remaining uncertainty is listed in the threat report: route reachability is a conservative checkpoint list, and breeding, trades, roaming RNG, and prerequisite-heavy statics are not fully route-modeled yet. | Use `python -m tools.boss_ai_preference serve --host 127.0.0.1 --port 8765` for the UI, `python -m tools.boss_ai_preference label ...` / `prefer ...` for CLI feedback, `python -m tools.boss_ai_preference report`, and `python -m tools.boss_ai_preference threat-report` after source or fixture changes. Next real V2 implementation should start with schema extension, then active queue; do not let an optimizer auto-land ROM behavior. |
| HANDOFF-001 | `WAIT` | Final durable handoff artifact | `UNTOUCHED` | 2026-04-29 | A fresh session feels invited into the work, not abandoned among evidence fragments. | Handoff pattern after 2026-04-29 cleanup: per-session prompts go in `.claude_handoffs/YYYY-MM-DD-HHMM-<slug>.md` (gitignored, see `CLAUDE.md`); durable judgment calls go in `decisions/`; per-session diary goes in `journal/`. Helper docs route through `docs/README.md`. | Not started. | Wait until a long run is ending, context is tight, or a checkpoint is about to be handed to another session. Then write a self-contained handoff naming changed files, verified commands, remaining uncertainty, and the exact first reads for the next session. |
| MASTERY-001 | `WAIT` | S-tier gym mastery bonus | `UNTOUCHED` | 2026-04-29 | Mastery feels seen: players who clear hard gyms cleanly find content that respects the effort, not just bigger numbers. | None yet — design only, captured from 2026-04-29 conversation. | Save-format question: needs ~13 per-gym flags and the repo has no save-version migration (`CLAUDE.md` RAM rules), so land this before first public release, not after. Open: whether the two criteria stack into one S-tier or run as two parallel tracks. | Trigger: clear Johto gyms 4-8 and all 8 Kanto gyms with (a) zero party faints during the battle, and (b) <=3 Pokemon in the party at battle start. Reward shape proposed as per-gym drip (small held items / consumables) plus a capstone hidden NPC giving an event-tier mon (e.g., Celebi with an otherwise-illegal-but-legal moveset). Detection hook lives at gym-leader battle-end script; flags fit in unused save bits. Spec not written. |
| BASELINE-001 | `REFERENCE` | Repo state and build-route baseline | `COMPLETE` | 2026-04-26 | Claims feel earned because source, build, and audit reality come before design confidence. | Captured `git status --short --branch`; inspected dirty diff for Red boss-tier/items plus helper-doc/audit/index wiring; confirmed WSL `make` route with `pokegold.gbc` and `pokesilver.gbc` current. | None for this baseline pass. | Keep as reference. Future gameplay claims should still name whether they came from source/audit/build or emulator play. |
| ROADMAP-001 | `REFERENCE` | Roadmap and helper-doc navigation setup | `COMPLETE` | 2026-04-26 | Future helpers feel oriented fast enough to protect the project instead of reinterpreting it. | `docs/project_roadmap.md`, `docs/README.md`, `docs/project_map.md`, `tools/audit/check_docs_navigation.py` | None. | Keep this as a routing board, not a long implementation diary. |
| TECHDEBT-001 | `REFERENCE` | Tech-debt and asm-guide audit track | `IN_PROGRESS` | 2026-05-04 | Engineering-cleanup work stays visible from the index instead of running invisibly in a parallel doc tree; future sessions know the track exists and where its source of truth lives. | The track has its own canonical status board at `tech_debt/STATUS.md` (4 open + 4 partial + 3 done + 1 disputed + 1 pending-trigger TD-### items as of 2026-05-04) and audit log at `tech_debt/AGENT_LOG.md`. Asm-guide pitfalls audited under AG-NN IDs in `tech_debt/ASM_GUIDE_AUDIT_2026-05-03.md` (AG-01 through AG-08). 2026-05-03 TD-005 Pattern 3 closed (`5e9b785f`, 45 bytes recovered in canary bank 0x0d). 2026-05-04 TD-012 closed (`a075b09a`, Makefile shell hacks → cross-platform Python helpers, SHA1 unchanged). 2026-05-04 AG-04 codemod (`93ff692e`, `ld a, 0` → `xor a` at 40 SAFE sites) + audit `bbf84e00`. 2026-05-04 AG-05 codemod (`e5091522`, `cp 0` → `and a` at 9 SAFE sites). 2026-05-04 AG-06 cold-path conversion (`8e93a627` + audio-doc `92a187a5`, 4 inline bank-switches → `rst Bankswitch`, 16 bytes recovered). 2026-05-04 AG-02 audit shipped (`d5b8b6a3`), surfaced 5 latent farcall a-clobber bugs all fixed (`13a6e3a3`), promoted to release-smoke floor (`17937d94`). 2026-05-04 AG-07 paralysis fail check fixed (`769d6dd4`). 2026-05-04 AG-08 `Battle_GetEffectiveMoveCategory` farcall a-clobber fixed (`a6a00ea8`). 2026-05-04 transitive register-clobber fixes in damagestats/regrowth path (`5e9b785f`) and `GetUserItem` de-clobber (`44ca3b29`). Audits in the release-smoke floor: `check_farcall_hl_clobber.py`, `check_farcall_a_clobber.py`, `check_ld_a_zero.py`, `check_cp_zero.py`. | TD-002 gated on `SAVE_FORMAT_VERSION` bump; TD-005 Pattern 2 gated on user WIP in `engine/pokemon/experience.asm`; TD-009a gated on user dead-write approval; TD-013 gated on user EXP-curve sign-off; AG-03 (39 boss.asm cross-bank calls) gated on trace-ROM manifest refresh and capture re-run after the `f2e18554` thunk fix. | This row is an **index pointer** — `tech_debt/STATUS.md` is the source of truth for TD-### state and `tech_debt/ASM_GUIDE_AUDIT_2026-05-03.md` for AG-NN state. Bump this row's `Last update` cell when any TD-### or AG-NN terminal entry lands and append a one-line summary; do not mirror detailed status here. The point of this row is to make the parallel track visible from the roadmap, not to replace its canonical docs. |
