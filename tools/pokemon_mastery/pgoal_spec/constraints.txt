- NEVER extract cases from validation or sealed_exam tier replays; verify_loop_state.py enforces this
- Before each iteration run `python tools/pokemon_mastery/loop_runner.py sync-preflight`: it rebases the working branch onto canonical master and runs the integrity gate, refusing on a dirty tree, a conflicting rebase, or a red gate. Don't start an iteration on a branch that hasn't passed preflight
- After the iteration's commits are green, run `python tools/pokemon_mastery/loop_runner.py land` to fast-forward the shared integration line (master) to HEAD. Landing finished, audit-passing loop work is ordinary delegated execution; leaving iterations stranded on a per-session branch is the divergence generator A3 exists to stop
- Pre-freeze context for fresh predictions is live_core.md plus the replay prompt plus at most one heuristic_core card plus retrieved cases; no archives, no web search, no future turns
- replay_index.jsonl is append-only; partitions are fixed at fetch time
- When stagnation triggers escalation, log via pgoal decision
- regression battery must stay 100% green; if a probe regresses, pause INGEST and refactor the relevant case before resuming
- Pause and ask user before any save-format-affecting changes (defensive)
- Per-iteration commits include a one-line message: pokemon-mastery-loop iter N phase replay_id
- The Predictor for fresh predictions is whichever session is running the iteration (Claude or Codex); they share the same case library
