# Move Progression Audit

This directory owns the generated HTML report for later weaker same-type
level-up moves.

Regenerate it with:

```powershell
python scripts\generate_move_progression_audit_html.py
```
