# Findings Detail — IMMUTABLE

**Compiled:** 2026-05-02
**Purpose:** Raw evidence behind each TD-### finding in
`TECH_DEBT_REPORT.md`. Lets future agents verify "did the fix actually
touch the right file:line?" without re-running exploration.

This file is **immutable**. Findings are dated to the audit pass; if a
file:line cited here turns out to be stale because the file already
changed, log it as `disputed:` in `AGENT_LOG.md` — do not edit this file.

---

## TD-001 — Bank exhaustion (full table from `docs/generated/dev_index.md`)

Source: `docs/generated/dev_index.md` Memory Summary section,
generated 2026-05-02.

| Region | Bank | Free bytes | Notes |
|--------|------|-----------:|-------|
| HRAM | 00 | 0 | Hardware temp variables |
| ROMX | 12 | 0 | Pic Pointers + Pics 1 (`org $4000`) |
| ROMX | 15 | 0 | Pics |
| ROMX | 17 | 0 | Pics |
| ROMX | 1b | 0 | Pics |
| ROMX | 1e | 0 | Pics |
| WRAMX | 01 | 0 | Boss AI state (reserve table below) |
| ROMX | 1c | 1 | Pics |
| ROMX | 1f | 1 | Unown Pic Pointers + Pics 12 (`org $4000`) |
| ROMX | 1a | 4 | Pics |
| ROMX | 0d | 6 | Battle engine code |
| ROMX | 0e | 6 | Boss AI + Late Gen Held Items + Type Passives co-located |

### WRAMX bank 1 reserve

Source: `docs/generated/dev_index.md` "Boss AI WRAM Reserve" section.

| Build configuration | Used bytes | Free in reserve | Total reserve |
|---------------------|-----------:|----------------:|--------------:|
| Normal | 104 | 36 | 140 |
| With `BOSS_AI_TRACE` | 123 | 17 | 140 |

Allocation breakdown (`ram/wram.asm:2534-2591`):
- `wBossAITier` start at `01:d72b`
- Core state (lines 2534-2551): 18 bytes
- `wBossAIPlausibleTypeMaskCache` (line 2552): 4 bytes
- `wBossAISeenPlayerSpeciesCount` (line 2553): 1 byte
- `wBossAISeenPlayerSpecies` (line 2554): 6 bytes (`PARTY_LENGTH`)
- `wBossAIRevealedMovesBitmap` (line 2555): 24 bytes (`PARTY_LENGTH × 4`)
- `wBossAILikelyTypeMaskCache` (line 2556): 4 bytes
- `wBossAISeenPlayerAliveMask` (line 2557): 1 byte
- `wBossAIRevealedMovesBitmapSpare` (line 2558): 3 bytes
- `wBossAIScorePtr` (line 2559): 2 bytes
- `wBossAISavedEnemyMoveStruct` (line 2560): 8 bytes (`MOVE_LENGTH`)
- Temp vars (lines 2561-2565): 5 bytes
- `wBossAITierWeightRow` (line 2566): 1 byte
- `wBossAISpeciesUsedMoves` (line 2567): 24 bytes
- Cache bytes (lines 2573-2576): 4 bytes
- Optional `BOSS_AI_TRACE` fields (lines 2578-2587): 19 bytes
- Reserve padding (line 2591):
  `ds 140 - (wBossAIStateEnd - wBossAITier)`

---

## TD-002 — Legacy save format v1→v2 cleanup

### `constants/misc_constants.asm:34-37`
```
$FF means "legacy save predating this marker" and is accepted only by v1
to absorb existing dev/playtest saves on first deploy. v2+ must NOT keep
the $FF accept path; only the current version should be accepted.
```

### `engine/menus/save.asm:640`
Inline comment: "legacy save predating the marker; v2+ must remove this"

### `engine/menus/save.asm:668`
Inline comment: "legacy save predating the marker; v2+ must remove this"

### `ram/sram.asm:105`
WRAM structure comment noting legacy save version handling.

### `ram/sram.asm:175`
WRAM structure comment noting legacy save version handling.

---

## TD-003 — Hard-coded `org` addresses in `layout.link`

| Bank | Lines | Section(s) | Pinned address |
|------|-------|------------|----------------|
| 0x12 | 72-75 | "Pic Pointers" + "Pics 1" | `org $4000` |
| 0x1f | 100-102 | "Unown Pic Pointers" + "Pics 12" | `org $4000` |
| 0x2e | 123-125 | "Pics 14" then "bank2E" | `org $6300` |
| 0x31 | 128-131 | "Sprites 2" then "bank31" | `org $7a40` |
| 0x3e | 160-163 | "bank3E" + "Shrink Pics" + "bank3E_2" | (named ordering, alignment-sensitive) |
| 0x7f | 260-262 | "Stadium 2 Checksums" + `ds $208` | `org $7df8` |

`Late Gen Held Items` section is declared in `main.asm:385` without an
explicit bank — linker-assigned alongside `Enemy Trainers` in bank 0x0e
(`main.asm:168`), which has 6 free bytes.

---

## TD-004 — `boss.asm` complexity metrics

- File: `engine/battle/ai/boss.asm`
- Lines: **7,006**
- Top-level routines (labels with `::`): 152
- Local labels (subroutine-scoped, starting with `.`): 560

### Localized duplication inside boss.asm

`.copy_possible_loop` and `.copy_likely_loop` are byte-identical loops:

```asm
; Lines 4927-4934
.copy_possible_loop
    ld a, [de]
    or [hl]
    ld [de], a
    inc hl
    inc de
    dec b
    jr nz, .copy_possible_loop

; Lines 4938-4945
.copy_likely_loop
    ld a, [de]
    or [hl]
    ld [de], a
    inc hl
    inc de
    dec b
    jr nz, .copy_likely_loop
```

`BossAI_SetSeenPlayerAliveBit` (lines 114-127) and
`BossAI_ClearSeenPlayerAliveBit` (lines 128-139) duplicate setup; only
operation differs (OR vs AND-complement).

`BossAI_ClearPlausibleMask` (lines 4901-4909) zeros 8 bytes manually:

```asm
ld hl, wBossAIPlausibleTypeMaskCache
xor a
ld [hli], a
ld [hli], a
ld [hli], a
ld [hli], a
ld hl, wBossAILikelyTypeMaskCache
ld [hli], a
ld [hli], a
ld [hli], a
ld [hl], a
```

Plausible/likely state-equality checks repeat across functions
(`boss.asm:4858-4880` and similar):

```asm
ld a, [wBossAIPlausibleTypeMaskSpecies]
cp b
jr nz, .recompute
ld a, [wBossAIPlausibleTypeMaskLevel]
cp c
ret z
```

---

## TD-005 — Duplicated boilerplate (file:line evidence)

### Pattern 1: item-check-and-return (12 instances in `late_gen_held_items.asm`)

8-instruction pattern:
```asm
push hl
push bc
callfar GetUserItem      ; or GetOpponentItem
ld a, b
pop bc
pop hl
cp HELD_XXXX
ret nz
```

Instances at lines: 24, 37, 50, 63, 84, 171, 311, 322, 344, 367, 529, 583.

Specifically `.ApplyChoiceBandBoost` (lines 35-45) and
`.ApplyChoiceSpecsBoost` (lines 47-57) differ only in the `cp HELD_*`
constant.

### Pattern 2: multiply/divide setup (18 instances across 3 files)

Pattern:
```asm
xor a
ldh [hMultiplicand + 0], a
ld a, [...]
ldh [hMultiplicand + 1], a
; load remaining operands ...
call Multiply
ld a, [hProduct + 0..3]
ldh [hDividend + 0..3], a   ; copy product to dividend
ld a, d                      ; or wherever divisor lives
ldh [hDivisor], a
call Divide
```

- `late_gen_held_items.asm`: 7 occurrences (.ApplyFractionToHL,
  .ApplyFractionToBC at lines 140-165, 257-290, plus inline)
- `type_passive_damage_mods.asm`: 6 occurrences
- `engine/pokemon/experience.asm`: 5 occurrences

### Pattern 3: hBattleTurn side-branch (100+ instances project-wide)

```asm
ldh a, [hBattleTurn]
and a
jr z, .player_side
ld hl, wEnemyMonXXX
jr .got_side
.player_side
ld hl, wBattleMonXXX
.got_side
```

Examples: `type_passive_damage_mods.asm:48-52, 505-509, 600-605` (for
`MOVE_TYPE`, `MOVE_POWER`, `MOVE_EFFECT` offsets).

### Other duplications in `type_passive_damage_mods.asm`

`.GetTypeContributionFromHL` (lines 230-258) duplicated by
`TypePassive_GetTypeContributionFromHL_Far` (lines 445-473). ~50 bytes
recoverable.

`.GetUserTypeContribution` / `.GetOpponentTypeContribution` (lines 263-283)
duplicated by `_Far` variants (lines 452-472).

`.GetUserHPAndMax` (lines 328-346) and `.GetOpponentHPAndMax` (lines
348-366) — identical logic, different addresses.

`TypePassive_GetLastCounterMoveCategory_Far` (lines 540-595) duplicates
Outrage-special-case logic from `TypePassive_GetEffectiveMoveCategory_Far`.
~70 bytes recoverable.

### Shell Bell unrolled shift (`late_gen_held_items.asm:353-356`)

```asm
srl b
rr c
srl b
rr c
srl b
rr c
```

Manual 3-bit shift (divide by 8). No named `SHELL_BELL_DIVISOR` constant.

---

## TD-006 — Magic numbers (file:line evidence)

### `engine/battle/type_passive_damage_mods.asm`
- Line 950-955: paralysis failure rate logic uses raw `25`, `41`, `40`,
  `21`, `20`. Comment: "baseline paralysis failure", "Electric type
  paralysis reduction". No constants in `constants/`.

### `engine/pokemon/experience.asm`
- Lines 56-58: `and $f0` then `swap a` — hex nibble extraction without
  named mask.
- Lines 92-94: `and $7f` — bottom-7-bits mask without named constant.
- Lines 198-206: `.NextJohtoGymCaps` table:
  ```asm
  db 14, 17, 21, 26, 34, 34, 34, 39
  ```
  Comment above maps gyms but data is unlabeled. Could be:
  ```asm
  db 14 ; FALKNER
  db 17 ; BUGSY
  db 21 ; WHITNEY
  db 26 ; MORTY
  ; ...
  ```

---

## TD-007 — Unreferenced labels enumeration

**Total: 419** unreferenced labels in source (counts approximate; based on
explore agent grep pass 2026-05-02).

### Beta map blocks (48 entries)

File: `data/maps/blocks.asm`. Examples:
- `BetaPlayersHouse2F_Blocks`
- `BetaSproutTower1_Blocks`
- `BetaBlackthornCity_Blocks`
- (45 more `Beta*_Blocks` entries)

### Unused Game Corner graphics

- `engine/games/card_flip.asm:5` — comment: "Graphics for an unused Game
  Corner game were meant to be here."

### Unused audio channel overrides (3 entries)

- `audio/engine.asm:335` — `.ch2_frequency_override`
- `audio/engine.asm:387` — `.ch3_frequency_override`
- `audio/engine.asm:486` — `.ch4_frequency_override`

### Unused battle utilities (10+ entries)

In `engine/battle/core.asm`:
- `GetHalfHP` (line 1971)
- `SwapBattlerLevels` (line 6504)
- `LoadHPExpBarGFX` (line 6788)
- `HandleSafariAngerEatingStatus` (line 7649)
- `FillEnemyMovesFromMoveIndicesBuffer` (line 8048)
- `PlayerPickedUpPayDayMoney` (line 8178)

### Unused map object / sprite functions

In `home/map_objects.asm`:
- `CopyMapObjectStruct` (line 303)
- `DeleteFollowerMapObject` (line 365)
- `SetVramState_SpriteUpdatesDisabled` (line 577)
- `ResetVramState_SpriteUpdatesDisabled` (line 582)

### Unused menu / UI utilities

In `home/menu.asm`:
- `Menu_DummyFunction` (line 298)
- `PlaceGenericTwoOptionBox` (line 388)
- `GetNthMenuStrings` (line 682)

### Unused text rendering

In `home/text.asm`:
- `GameFreakText` (line 128)
- `DummyChar` (line 169)
- `PokeFluteTerminator` (line 571)
- `TextCommand_CRY` (line 843)

### Unused per-map content (100+ entries)

Examples (full enumeration in source via grep `: ; unreferenced`):
- `UnusedWoosterScript` (`maps/AzaleaTown.asm:141`)
- `UnusedMissingDaughterText` (`maps/EcruteakCity.asm:87`)
- `ElmsHouseLabFoodText` (`maps/ElmsHouse.asm:46`)
- `TinTower9FUnusedHoOhText` (`maps/TinTower9F.asm:6`)
- `UnusedBillsHouseSignText` (`maps/Route25.asm:426`)

### Stale reward text (already guarded)

Pattern: labels matching `^.*Received(TM|HM)\w*:\s*;\s*unreferenced\b`.
Guard: `tools/audit/check_release_smoke.py:286-295`. Recently cleaned in
late April 2026 — see `journal/2026-04-27_*` entries.

### Beta features kept for reference

- `engine/games/slot_machine.asm:255-257` — `AnimateSlotReelIcons`,
  comment: "This animation was present in pokegold-spaceworld."

### Other markers

- `engine/battle/effect_commands.asm:82` — commented-out alternative
  expansion, kept as reference.
- `constants/hardware.inc:146` — `; -- $FF03 is unused --` placeholder.

---

## TD-008 — RGBDS pin

Pin location citations:
- `.github/workflows/main.yml:9` — `rgbds_version: v1.0.1`
- `.github/workflows/main.yml:22` — used in download step
- `.github/workflows/main.yml:78` — used in second download step
- `CLAUDE.md` — build command references `rgbds-1.0.1/`
- Multiple files in `audit/` reference `rgbds-1.0.1/` paths

`rgbdscheck.asm` validates minimum version (v1.0.0+), not current.

---

## TD-009 — Unused RAM fields enumeration

### `ram/wram.asm`

| Field | Line | Size | Status |
|-------|------|------|--------|
| `wUnusedBCDNumber` | 17 | 1 byte | "BCD value, dummied out"; no refs |
| `wUnusedMusicF9Flag` | 72 | 1 byte | Writes only (`audio/engine.asm`); no reads |
| `wUnusedScriptByte` | 120 | 1 byte | No refs |
| `wUnusedMapBuffer` | 272-273 | **24 bytes** | Marked unused in layout.link comment |
| `wUnusedGameboyPrinterSafeCancelFlag` | (search) | 1 byte | No refs |
| `wUnusedPikachuFrameset` | (search) | 1 byte | No refs |
| `wUnusedJigglypuffNoteXCoord` | (search) | 1 byte | No refs |
| `wUnusedMysteryGiftStagedDataLength` | (search) | 1 byte | No refs |
| `wUnusedLinkAction` | (search) | 1 byte | No refs |
| `wUnusedPokedexByte` | (search) | 1 byte | No refs |
| `wUnusedPokegearByte` | (search) | 1 byte | No refs |
| `wUnusedBillsPCData` | (search) | 3 bytes | No refs |
| `wUnusedMovementBufferBank` | (search) | 1 byte | No refs |
| `wUnusedMovementBufferPointer` | (search) | 2 bytes | No refs |

### `ram/hram.asm`

| Field | Line | Size | Status |
|-------|------|------|--------|
| `hUnusedByte` | 31 | 1 byte | No refs |
| `hUnusedBackup` | 157 | 1 byte | No refs |

### Confirmed actually used (DO NOT REMOVE despite "Unused" name)

- `wUnusedSlotReelIconDelay` — referenced by `engine/games/slot_machine.asm`
- `wUnusedSGB1eColorOffset` — referenced by `engine/gfx/sgb_layouts.asm`
- `wUnusedNamesPointer` — referenced by `engine/link/init_list.asm`

---

## TD-010 — `.gitignore` cruft enumeration

`/.gitignore` entries citing non-existent paths:
- Line 23: `rgbds-1.0.1/`
- Lines 24-26: `rgbds-win64.zip`, `rgbds-win64*.zip`, `rgbds-*/`
- Line 42: `.local/`
- Line 44: `.claude_handoffs/`
- Line 45: `.rebalance_chain/`

Duplicate patterns:
- Lines 54-56 duplicate lines 51-53 (`.sav`, `.rtc`, `.state`).

Overly broad:
- Line 47: `dist/*.gb` — `dist/` only contains `.bps` patches and
  `checksums.txt`, not ROM binaries.

---

## TD-011 — Unused script

- `scripts/export_changes_by_category.py`
  - No references in `tools/audit/`
  - No references in `scripts/` itself
  - No references in `Makefile`
  - No references in `.github/workflows/`
  - No references in `docs/`

A sibling exists: `scripts/export_changes_from_manifest.py` — actively
used in changelog generation.

---

## TD-012 — Makefile shell hacks (file:line evidence)

`/Makefile`:

- Lines 248-249:
  ```makefile
  gfx/pokemon/%/back_gold.2bpp: gfx/pokemon/%/back.2bpp ; cp -f $^ $@
  gfx/pokemon/%/back_silver.2bpp: gfx/pokemon/%/back.2bpp ; cp -f $^ $@
  ```

- Lines 279-281:
  ```makefile
  gfx/intro/fire1.2bpp: ... ; cat $^ > $@
  gfx/intro/fire2.2bpp: ... ; cat $^ > $@
  ```

- Lines 353-354:
  ```makefile
  gfx/sgb/gold_border.sgb.tilemap: gfx/sgb/gold_border.bin ; tr < $< -d '\000' > $@
  ```

- Line 140 (related comment):
  "Ignore the checksums added by tools/stadium at the end of the ROM" —
  hardcoded byte offset `0x1ffdf8:0x208`. Tied to TD-003's bank 0x7f
  pin.

---

## TD-013 — `experience.asm` evidence

File: `engine/pokemon/experience.asm` (298 lines)

`CalcExpAtLevel` (lines 82-160):
- Pushes cubic term: lines 82-84 (3 separate `push af`)
- Pushes quadratic term: lines 97-100 (4 pushes)
- Pops + reconstructs: lines 118-144 (9 separate pop operations)
- Total: 10+ push/pop pairs for stack arithmetic

Repeated nibble extraction:
- Lines 56-58: `ld a, [hli] / and $f0 / swap a` — high nibble
- Lines 92-94: `ld a, [hl] / and $7f` — low 7 bits

Unrolled level-squaring routine (lines 172-180):
```asm
.LevelSquared:
    xor a
    ldh [hMultiplicand + 0], a
    ldh [hMultiplicand + 1], a
    ld a, d
    ldh [hMultiplicand + 2], a
    ldh [hMultiplier], a
    jp Multiply
```
Could be a generic `DoSquare` utility shared with other math-heavy
routines.

---

## Verification commands by finding

For checking whether a fix actually addressed the cited evidence:

| Finding | Re-verify with |
|---------|----------------|
| TD-001 | `python scripts/generate_dev_index.py --rom pokegold` then read the Memory Summary table |
| TD-002 | `grep -n 'legacy save' constants/misc_constants.asm engine/menus/save.asm ram/sram.asm` |
| TD-003 | `grep -n 'org $' layout.link` |
| TD-004 | `wc -l engine/battle/ai/boss.asm` |
| TD-005 | `grep -c 'callfar GetUserItem\|callfar GetOpponentItem' engine/battle/late_gen_held_items.asm` |
| TD-006 | Inspect `engine/battle/type_passive_damage_mods.asm:950-955`, `engine/pokemon/experience.asm:56-58,92-94,198-206` |
| TD-007 | `grep -rn '; unreferenced' --include='*.asm' \| wc -l` |
| TD-008 | `grep -rn 'rgbds_version\|rgbds-1\.0\.1' .github/ CLAUDE.md` |
| TD-009 | `grep -n 'Unused' ram/wram.asm ram/hram.asm` |
| TD-010 | Compare `.gitignore` entries against actual filesystem with `ls`/`find` |
| TD-011 | `grep -rn 'export_changes_by_category' --include='*.py' --include='*.md' --include='Makefile'` |
| TD-012 | `grep -n 'cp -f\|cat \$\^\|tr <' Makefile` |
| TD-013 | `grep -c 'push af\|pop af' engine/pokemon/experience.asm` |

End of immutable findings detail.
